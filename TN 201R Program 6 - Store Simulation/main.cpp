#include <iostream>
#include <cstdlib>
#include "linkedList.h"
#include "queue.h"
#include "queueData.h"
using namespace std;

int main() {
	srand(time(0));
	bool running = true;
	while (running) {
		for (int minutes = 0; minutes < 720; minutes++) {
			int newCustomers = rand() % 3 + 1;
		}
	}
}